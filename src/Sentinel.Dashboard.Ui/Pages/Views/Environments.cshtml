<template id="environments">
    <div class="flex justify-center">
        <div class="p-2">
            <br/>

            <span class="text-4xl text-slate-900">Select an environment</span>
            
            <p>&nbsp;</p>

            <div class="flex justify-center">
                <div>
                    <ul class="list-none, pl-0">
                        <li v-for="env in environments">
                            <a class="menu-link-brand font-bold no-underline text-4xl max-w-full block p-2" :href="`/environments/${env}/overview`"> {{ env }}</a>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</template>

<script type="module">
    components["environments"] = {
        template: "#environments",
        data() {
            return {
                pods: {},
                podData: [],
            }
        },
        props: ['namespace', 'search'],        
        computed: {},
        async mounted() {
            var host = window.location.origin;
        },
    };
    
    routes.push({path:'/', component: components["environments"]})    
</script>

<style>
</style>